apiVersion: v1
kind: PersistentVolume
metadata:
  name: mongodb-producao-pv
  labels:
    type: local
    name: mongodb-producao-pv
spec:
  storageClassName: manual
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: /data/mongodbprod
